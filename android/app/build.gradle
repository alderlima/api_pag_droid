plugins {
    id "com.android.application"
    id "org.jetbrains.kotlin.android"
    // O plugin do Flutter deve vir por último
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.exemplo.api_pag_droid" // Use o seu package name
    compileSdk = 35 // Atualizado para suportar Kotlin 2.1

    defaultConfig {
        applicationId = "com.exemplo.api_pag_droid"
        minSdk = 21
        targetSdk = 35
        versionCode = 1
        versionName = "1.0"
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug // Ajuste para sua chave de produção
            minifyEnabled false
            shrinkResources false
        }
    }
    
    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_11
        targetCompatibility = JavaVersion.VERSION_11
    }

    kotlinOptions {
        jvmTarget = "11"
    }
}
